CREATE TABLE QUALIFICATION_RESERVATIONS (
    ID NUMBER,
    ID_RESERVATION NUMBER,
    CALIFICATION NUMBER,
    MESSAGE VARCHAR2(250),
    PRIMARY KEY (ID),
    FOREIGN KEY (ID_RESERVATION) REFERENCES RESERVATIONS(ID),
    check (CALIFICATION between 0 and 5) 
);